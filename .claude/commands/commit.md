# Test, Commit and Push

1. Run TEST_CMD from `.claude/commands.env` - must pass
2. Review changes with git status and git diff
3. Stage files and create meaningful commit message
4. Commit and push to remote