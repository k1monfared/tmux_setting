# Claude Configuration

This directory contains Claude-specific settings and configurations.

## Files
- `hooks.json` - Hook configurations for automated actions
- `settings.json` - Claude preferences and output styles